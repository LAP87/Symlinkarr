# Tasklist - 2026-04-17

Source: current repo state after explainability pass, db decomposition, release `v1.0.0-rc.11`, and theme/web UI polish.

This tasklist reflects the current preferred execution order.
Our structural and operator-UX priorities come first.
Gemini audit ideas are captured below as secondary tracks, not ignored.

## Primary Track

### 1. Split `cleanup_audit.rs`

- Current size: ~4587 lines
- Reason: largest remaining hotspot in the repo
- Goal: separate audit, classification, preview/remediation, and tests into smaller modules
- Constraint: no behavioral rewrite during the split

#### Progress

- [x] Mapped current `cleanup_audit.rs` shape and identified the first low-risk split
- [x] Move `cleanup_audit` tests into `src/cleanup_audit/tests.rs`
- [x] Move prune/quarantine execution helpers into a dedicated submodule
- [x] Move title/ownership/classification helpers into a dedicated submodule
- [x] Move report/prune data types into a dedicated submodule if the file is still too dense afterward

### 2. Split `config.rs`

- Current size: ~3022 lines
- Reason: broad fan-in and high change risk
- Goal: separate defaults, load/resolve, validation, and tests
- Constraint: preserve current config behavior and secret resolution semantics

#### Progress

- [x] Mapped current `config.rs` shape and identified the first low-risk split
- [x] Move `config` tests into `src/config/tests.rs`
- [x] Move default-value helpers and `Default` impls into a dedicated submodule
- [x] Move load/resolve and secret-resolution helpers into a dedicated submodule
- [x] Move validation helpers into a dedicated submodule

### 3. Thin the web layer

- Targets:
- `src/web/api/mod.rs` (~3710 lines)
- `src/web/handlers.rs` (~3255 lines)
- Reason: both are now bigger architectural hotspots than `db.rs`
- Goal: break out endpoint families / handler groups so web behavior is easier to reason about and test

#### Progress

- [x] Mapped current web-layer hotspots and identified the first low-risk split
- [x] Move `src/web/api/mod.rs` tests into `src/web/api/tests.rs`
- [x] Split scan/history API endpoints into a dedicated module
- [x] Split cleanup/remediation API endpoints into a dedicated module
- [x] Split admin/misc API endpoints into a dedicated module
- [x] Start the same decomposition pass for `src/web/handlers.rs`
- [x] Move `src/web/handlers.rs` tests into `src/web/handlers/tests.rs`
- [x] Split scan/history page handlers into a dedicated module
- [x] Split cleanup/remediation page handlers into a dedicated module
- [x] Split config/discover/backup page handlers into a dedicated module

### 4. Keep taking small web UI fixes during live testing

- Scope:
- layout overflow
- picker behavior
- wording/copy cleanup
- spacing/readability issues
- Goal: treat concrete operator-facing paper cuts as cheap, high-signal wins while broader refactors continue

### 5. Split `auto_acquire.rs`

- Current size: ~3286 lines
- Reason: largest remaining hotspot after the db/config/web structural pass
- Goal: separate queue orchestration, provider lookup/ranking, and tests into smaller modules
- Constraint: preserve current queue semantics and explainability reason codes

#### Progress

- [x] Mapped current `auto_acquire.rs` shape and identified the first low-risk split
- [x] Move `auto_acquire` tests into `src/auto_acquire/tests.rs`
- [x] Split queue persistence/submission helpers into a dedicated submodule
- [x] Split DMM lookup and ranking helpers into a dedicated submodule
- [ ] Split anime candidate scoring helpers into a dedicated submodule

### 6. Continue explainability polish only where it improves operations directly

- Focus on surfacing existing "why not?" data better
- Do not expand into a new feature program

## Secondary Track

These are good ideas, but should come after the primary structural pass unless one becomes urgent.

### 7. Formal `MediaServerAdapter` trait

- From Gemini audit
- Good architectural direction
- Better done after current file/module boundaries are cleaner

### 8. Anime mapping override workflow

- From Gemini audit
- Good candidate for future operator tooling
- Not current priority because it is feature breadth, not structural hardening

### 9. Live log viewer in dashboard

- From Gemini audit
- Useful operator UX, but larger than it looks
- Should wait until web/API decomposition reduces coupling

### 10. Streaming guard via Tautulli

- From Gemini audit
- Interesting reliability feature
- Should wait until current architectural cleanup is further along

## Recommended Order

1. `cleanup_audit.rs`
2. `config.rs`
3. `src/web/api/mod.rs`
