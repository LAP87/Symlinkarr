# Tasklist - 2026-04-17

Source: current repo state after explainability pass, db decomposition, release `v1.0.0-rc.11`, and theme/web UI polish.

This tasklist reflects the current preferred execution order.
Our structural and operator-UX priorities come first.
Gemini audit ideas are captured below as secondary tracks, not ignored.

## Primary Track

### 1. Split `cleanup_audit.rs`

- Current size: ~4587 lines
- Reason: largest remaining hotspot in the repo
- Goal: separate audit, classification, preview/remediation, and tests into smaller modules
- Constraint: no behavioral rewrite during the split

#### Progress

- [x] Mapped current `cleanup_audit.rs` shape and identified the first low-risk split
- [x] Move `cleanup_audit` tests into `src/cleanup_audit/tests.rs`
- [x] Move prune/quarantine execution helpers into a dedicated submodule
- [x] Move title/ownership/classification helpers into a dedicated submodule
- [x] Move report/prune data types into a dedicated submodule if the file is still too dense afterward

### 2. Split `config.rs`

- Current size: ~3022 lines
- Reason: broad fan-in and high change risk
- Goal: separate defaults, load/resolve, validation, and tests
- Constraint: preserve current config behavior and secret resolution semantics

#### Progress

- [x] Mapped current `config.rs` shape and identified the first low-risk split
- [x] Move `config` tests into `src/config/tests.rs`
- [x] Move default-value helpers and `Default` impls into a dedicated submodule
- [x] Move load/resolve and secret-resolution helpers into a dedicated submodule
- [x] Move validation helpers into a dedicated submodule

### 3. Thin the web layer

- Targets:
- `src/web/api/mod.rs` (~3710 lines)
- `src/web/handlers.rs` (~3255 lines)
- Reason: both are now bigger architectural hotspots than `db.rs`
- Goal: break out endpoint families / handler groups so web behavior is easier to reason about and test

#### Progress

- [x] Mapped current web-layer hotspots and identified the first low-risk split
- [x] Move `src/web/api/mod.rs` tests into `src/web/api/tests.rs`
- [x] Split scan/history API endpoints into a dedicated module
- [x] Split cleanup/remediation API endpoints into a dedicated module
- [x] Split admin/misc API endpoints into a dedicated module
- [x] Start the same decomposition pass for `src/web/handlers.rs`
- [x] Move `src/web/handlers.rs` tests into `src/web/handlers/tests.rs`
- [x] Split scan/history page handlers into a dedicated module
- [x] Split cleanup/remediation page handlers into a dedicated module
- [x] Split config/discover/backup page handlers into a dedicated module

### 4. Keep taking small web UI fixes during live testing

- Scope:
- layout overflow
- picker behavior
- wording/copy cleanup
- spacing/readability issues
- Goal: treat concrete operator-facing paper cuts as cheap, high-signal wins while broader refactors continue

#### Progress

- [x] Add lightweight disclosure panels on dashboard/scan/status/cleanup so secondary guidance and backlog detail stop competing with primary operator actions

### 5. Split `auto_acquire.rs`

- Current size: ~3286 lines
- Reason: largest remaining hotspot after the db/config/web structural pass
- Goal: separate queue orchestration, provider lookup/ranking, and tests into smaller modules
- Constraint: preserve current queue semantics and explainability reason codes

#### Progress

- [x] Mapped current `auto_acquire.rs` shape and identified the first low-risk split
- [x] Move `auto_acquire` tests into `src/auto_acquire/tests.rs`
- [x] Split queue persistence/submission helpers into a dedicated submodule
- [x] Split DMM lookup and ranking helpers into a dedicated submodule
- [x] Split anime candidate scoring helpers into a dedicated submodule

### 6. Split `repair.rs`

- Current size: ~2573 lines
- Reason: next clear hotspot with distinct safety, parser, repair, scoring, and test sections
- Goal: separate tests and scoring/parser helpers into smaller modules without changing repair behavior
- Constraint: preserve current repair safety guards and matching semantics

#### Progress

- [x] Mapped current `repair.rs` shape and identified the first low-risk split
- [x] Move `repair` tests into `src/repair/tests.rs`
- [x] Split scoring helpers into a dedicated submodule
- [x] Split TRaSH parsing helpers into a dedicated submodule if the parent file is still too dense
- [x] Split dead-link drift/safety helpers into a dedicated submodule if the parent file is still too dense

### 7. Split `backup.rs`

- Current size: ~2424 lines
- Reason: next large integrity-critical module with clear data, restore, and test sections
- Goal: separate tests and low-risk restore/path helper families into smaller modules without changing backup behavior
- Constraint: preserve restore validation and root-safety semantics

#### Progress

- [x] Mapped current `backup.rs` shape and identified the first low-risk split
- [x] Move `backup` tests into `src/backup/tests.rs`
- [x] Split restore/path validation helpers into a dedicated submodule
- [x] Split manifest/path naming helpers into a dedicated submodule if the parent file is still too dense

### 8. Split `commands/report.rs`

- Current size: ~2615 lines
- Reason: largest remaining hotspot after the current backup split
- Goal: separate tests and report helper families into smaller modules without changing report output
- Constraint: preserve report JSON/text output and anime remediation reporting behavior

#### Progress

- [x] Mapped current `commands/report.rs` shape and identified the first low-risk split
- [x] Move `report` tests into `src/commands/report/tests.rs`
- [x] Split anime duplicate/remediation helpers into a dedicated submodule
- [x] Split path compare / diff helpers into a dedicated submodule if the parent file is still too dense

### 9. Split `matcher.rs`

- Current size: ~2363 lines
- Reason: largest remaining hotspot after the current report split
- Goal: separate tests and matcher helper families into smaller modules without changing matching behavior
- Constraint: preserve skip reasons, ambiguity handling, and anime identity behavior

#### Progress

- [x] Mapped current `matcher.rs` shape and identified the first low-risk split
- [x] Move `matcher` tests into `src/matcher/tests.rs`
- [x] Split metadata/cache helpers into a dedicated submodule
- [x] Split scoring/prefilter helpers into a dedicated submodule

### 10. Split `commands/cleanup.rs`

- Current size: ~2258 lines
- Reason: next clear CLI hotspot with distinct dead-link, audit, anime remediation, and test sections
- Goal: separate tests and low-risk anime remediation/helper families into smaller modules without changing cleanup behavior
- Constraint: preserve prune/remediation safety gates, report output, and media-server refresh behavior

#### Progress

- [x] Mapped current `commands/cleanup.rs` shape and identified the first low-risk split
- [x] Move `commands/cleanup` tests into `src/commands/cleanup/tests.rs`
- [x] Split anime remediation report/filter helpers into a dedicated submodule
- [x] Split Plex DB path/report loading helpers into a dedicated submodule if the parent file is still too dense

### 11. Split `web/mod.rs`

- Current size: ~2271 lines
- Reason: current largest remaining hotspot; mixes route construction, background job state, auth/session guards, cleanup helpers, startup, and tests
- Goal: separate tests and low-risk helper families into smaller modules without changing web behavior
- Constraint: preserve auth/session semantics, route wiring, and startup behavior

#### Progress

- [x] Mapped current `web/mod.rs` shape and identified the first low-risk split
- [x] Move `web` module tests into `src/web/tests.rs`
- [x] Split auth/session/browser-mutation helpers into a dedicated submodule
- [x] Split cleanup/library-scope helper families into a dedicated submodule if the parent file is still too dense

### 12. Split `linker.rs`

- Current size: ~1736 lines
- Reason: next structural hotspot with core link validation, naming/path helpers, and inline tests
- Goal: separate tests and low-risk filename/path helper families into smaller modules without changing link behavior
- Constraint: preserve link safety checks, skip reasons, and naming behavior

#### Progress

- [x] Mapped current `linker.rs` shape and identified the first low-risk split
- [x] Move `linker` tests into `src/linker/tests.rs`
- [x] Split filename/path helper families into a dedicated submodule if the parent file is still too dense

### 13. Split `media_servers/mod.rs`

- Current size: ~1803 lines
- Reason: next structural hotspot with refresh orchestration, deferred queue handling, and inline tests
- Goal: separate tests and low-risk refresh/deferred-queue helper families into smaller modules without changing refresh behavior
- Constraint: preserve media-server invalidation semantics and deferred refresh queue behavior

#### Progress

- [x] Mapped current `media_servers/mod.rs` shape and identified the first low-risk split
- [x] Move `media_servers` tests into `src/media_servers/tests.rs`
- [x] Split deferred refresh queue/path helper families into a dedicated submodule if the parent file is still too dense

### 14. Split `web/templates.rs`

- Current size: ~2041 lines
- Reason: current largest remaining non-test hotspot; mixes presentation mappers, skip-reason humanization, cleanup/remediation views, and inline tests
- Goal: separate tests and low-risk presentation helper families into smaller modules without changing rendered output
- Constraint: preserve current Askama rendering, skip-reason wording, and cleanup/remediation summaries

#### Progress

- [x] Mapped current `web/templates.rs` shape and identified the first low-risk split
- [x] Move `web/templates` tests into `src/web/templates/tests.rs`
- [x] Split skip-reason presenter/grouping helpers into a dedicated submodule if the parent file is still too dense

### 15. Continue explainability polish only where it improves operations directly

- Focus on surfacing existing "why not?" data better
- Do not expand into a new feature program

### 16. Split `commands/scan.rs`

- Current size: ~989 lines
- Reason: current CLI hotspot for scan orchestration, telemetry aggregation, output helpers, and inline tests
- Goal: separate tests and low-risk telemetry/helper families into smaller modules without changing scan behavior
- Constraint: preserve scan sequencing, telemetry output, and skip-reason aggregation

#### Progress

- [x] Mapped current `commands/scan.rs` shape and identified the first low-risk split
- [x] Move `commands/scan` tests into `src/commands/scan/tests.rs`
- [x] Split telemetry/skip-reason helper families into a dedicated submodule if the parent file is still too dense
- [x] Replace string-mirroring telemetry tests with assertions against production formatting helpers

### 17. Split `source_scanner.rs`

- Current size: ~877 lines
- Reason: current parser hotspot mixing cache/filesystem scanning and a large inline parser regression suite
- Goal: move the regression suite out of the parser implementation and stop there unless the parent file stays clearly too dense
- Constraint: preserve parser behavior and source scanning semantics

#### Progress

- [x] Mapped current `source_scanner.rs` shape and identified the low-risk split
- [x] Move `source_scanner` tests into `src/source_scanner/tests.rs`

### 18. Test quality pass

- Reason: several tests still mirror copy or reimplement production formatting instead of asserting behavior where it can regress
- Goal: replace brittle smoke tests with route/UI-contract assertions and route low-signal string checks through production helpers where possible
- Constraint: do not delete coverage unless an equal-or-better assertion replaces it

#### Progress

- [x] Replace string-mirroring scan telemetry tests with assertions against production formatting helpers
- [x] Replace broad copy-based web smoke tests with per-page UI-contract assertions
- [x] Replace remaining web auth/config success-path copy checks with route/form contract assertions
- [x] Merge or remove redundant repair parser/helper tests when broader variants already cover the same cases
- [x] Add high-value restore tests for standalone and auto-restore target resolution
- [x] Add high-value startup auto-restore selection and guard tests in `main.rs`
- [x] Add high-value startup auto-restore failure-path coverage when restored config is still invalid
- [x] Add high-value bootstrap command tests for list-only, create, and existing-config paths
- [x] Add high-value standalone restore wrapper tests so `--dry-run` and `--list` stay non-destructive
- [x] Add a no-config web route contract test so the first-run setup page keeps both restore and bootstrap paths visible

### 18b. Operator activity / live run feed

- Reason: the backend now tracks active web jobs and latest outcomes, but the dashboard still makes operators infer too much from separate pages
- Goal: surface scan, cleanup, and repair activity in one polling dashboard feed before attempting richer real-time UX
- Constraint: start with HTMX polling and existing state/outcomes; do not jump straight to raw log streaming or WebSockets

#### Progress

- [x] Add a first-pass HTMX-polled dashboard activity feed for active scan/cleanup/repair work and latest finished outcomes
- [x] Add a dashboard "needs attention" layer for failed background work, dead links, blocked queue pressure, and deferred refresh backlog
- [x] Add suggested next-step guidance and more precise drilldown links so dashboard priorities point operators toward the right follow-up page

## Secondary Track

These are good ideas, but should come after the primary structural pass unless one becomes urgent.

### 19. Formal `MediaServerAdapter` trait

- From Gemini audit
- Good architectural direction
- Better done after current file/module boundaries are cleaner

### 20. Anime mapping override workflow

- From Gemini audit
- Good candidate for future operator tooling
- Not current priority because it is feature breadth, not structural hardening

### 21. Live log viewer in dashboard

- From Gemini audit
- Useful operator UX, but larger than it looks
- Should wait until web/API decomposition reduces coupling

### 22. Streaming guard via Tautulli

- From Gemini audit
- Interesting reliability feature
- Should wait until current architectural cleanup is further along

## Recommended Order

1. `cleanup_audit.rs`
2. `config.rs`
3. `src/web/api/mod.rs`
4. `commands/cleanup.rs`
5. `web/mod.rs`
6. `linker.rs`
7. `media_servers/mod.rs`
8. `web/templates.rs`
9. `commands/scan.rs`
10. `source_scanner.rs`
